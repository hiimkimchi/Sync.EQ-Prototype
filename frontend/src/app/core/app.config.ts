import { ApplicationConfig } from '@angular/core';

// This file is currently not being used as the configuration is directly in main.ts
export const appConfig: ApplicationConfig = {
  providers: []
};
